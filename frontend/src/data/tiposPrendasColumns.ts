import type { Column } from '@/interfaces/DataColumn'

export const tiposPrendasColumns: Column[] = [
    {key: 'index', label: '#'},
    {key: 'nombre', label: 'Nombre'},
    {key: 'options', label: 'Opciones'}
]